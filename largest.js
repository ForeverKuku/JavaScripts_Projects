function findLargest(num1, num2, num3) {
    return Math.max(num1, num2, num3);
  }


  // Example usage:
let largestNumber = findLargest(1, 2, 3);
console.log("The largest number is: " + largestNumber);

